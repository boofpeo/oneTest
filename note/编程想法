		脏读，读到未提交的数据。
	不可重复读，读到已提交的数据，两次读不一样。
	幻读，读的时候，事务A修改，事务B又重新插入。 隔离级别，读未提交，读已提交，可重复读（事务加锁，不允许修改，操作对象），串行化

	面向接口编程：
			以接口提供抽象化的方法。
	目的：优化设计的结构，减少维护的难度和成本，提供扩展以及复用的能力。所以才用抽象类或者是接口的方式去构造系统的结构。
			  难题是如何在具体实现的执行过程中，执行需要的子类对象。
			  后期绑定，使用对象的存储信息来计算确认方法体的绝对地址。（第一章的猜测，编译器计算划分区块，保证方法不冲突）
		接口继承，可以使用，使用上层接口，来确认调用的方法，比如，几何图形接口，存在面积和周长方法，规则几何图形接口，继承几何图形，并且新增对称轴等方法。
	在调用面积和方法时，我们可以用复用几何图形接口来调用方法。
		过程，和具体方法分离，过程，或者说结构以接口的形式构建，具体允许执行的结构，以方法填充运行。
		在类内定义的变量存在默认值，编译不报错，但是如果是方法内的未初始化的时候，引用都会报错。

		equals 默认比较引用（Object），如果自己定义的类中 未覆盖该方法。结果显而易见。